<view class="pt25r pb25r fs32r">
  <view class="mb20r {{formInfo.required == 1 ? 'need' : ''}}">{{formInfo.name}}</view>
  <textarea class="rds20r bde5 w93 db fs26r" style="min-height:450rpx;padding:20rpx;" value="{{formInfo.defaultValue}}" placeholder="请输入{{formInfo.name}}" placeholder-style=""
    placeholder-class="textarea-placeholder" disabled="{{type == 'detail'}}" maxlength="300" auto-focus="{{false}}" focus="{{false}}"
    bindinput="inputTextArea"
  >
  </textarea>
</view>